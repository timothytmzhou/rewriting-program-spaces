;; pow 10 (-15) * (66743 * m1 * m2) / (pow r 2)

(let m1 (Var "m1"))
(let m2 (Var "m2"))
(let r  (Var "r"))

(let start
  (Mul 
    (Pow (Num 10) (Neg (Num 15)))
    (Div
      (Mul (Mul (Num 66743) m1) m2)
      (Pow r (Num 2))
    )
  )
)
